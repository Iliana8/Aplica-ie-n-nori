FROM python:3.11-slim

WORKDIR /app

COPY app/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/proxy_server.py .

EXPOSE 9000

CMD ["uvicorn", "proxy_server:app", "--host", "0.0.0.0", "--port", "9000", "--workers", "4"]
